name: Validate Recipe
description: A GitHub Action to validate a recipe file and fetch its properties
author: Your Name
inputs:
  recipe-file:
    description: The path to the recipe file
    required: true
outputs:
  recipe-properties:
    description: The properties extracted from the recipe file
runs:
  using: node12
  main: index.js
