name: Planner Action
description: Validates JSON and extracts content for Planner workflow
inputs:
  json-file:
    description: Path to the JSON file
    required: true
outputs:
  substrate-tag:
    description: The Substrate Tag
  substrate-name:
    description: The Substrate Name
  substrate-registry:
    description: The Substrate Registry
  substrate-auth:
    description: The Substrate Auth
runs:
  using: "node12"
  main: "index.js"
